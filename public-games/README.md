Place future hosted game builds here under `public-games/<slug>/`.

If a project in `catalog.js` is marked `publicStatus: "live"` and a matching folder exists here, the build script will copy it into `dist/games/<slug>/`.

